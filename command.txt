/* Windows */
npm init -y --> initialized package for modules
npm install puppeteer --> install puppeteer for easy scrap
npm install minimist --> install minimist package

/* Linux */
chmod 777 $path --> 777 gives all rights be careful
sudo chown -R $user $path --> change floder owner
cd $path --> move
mkdir mp3 --> create mp3 folder 
sudo chmod 777 mp3/ --> gives all rights at mp3 folder
npm init -y --> initialized package for modules
sudo npm install -g puppeteer --unsafe-perm=true -allow-root && sudo apt install chromium-browser -y --> install puppeteer for easy scrap
npm i puppeteer --save --> install puppeteer for easy scrap
sudo apt update && sudo apt install -y gconf-service libgbm-dev libasound2 libatk1.0-0 libc6 libcairo2 libcups2 libdbus-1-3 libexpat1 libfontconfig1 libgcc1 libgconf-2-4 libgdk-pixbuf2.0-0 libglib2.0-0 libgtk-3-0 libnspr4 libpango-1.0-0 libpangocairo-1.0-0 libstdc++6 libx11-6 libx11-xcb1 libxcb1 libxcomposite1 libxcursor1 libxdamage1 libxext6 libxfixes3 libxi6 libxrandr2 libxrender1 libxss1 libxtst6 ca-certificates fonts-liberation libappindicator1 libnss3 lsb-release xdg-utils wget --> install all dependencies
sudo npm install minimist --> install minimist package
sudo chmod 777 head/ --> gives all rights at head folder
cd head/ --> move into head folder
mkdir mp3 --> create a mp3/ folder into head
sudo chmod 777 mp3/ --> gives all rights at mp3/ folder
cd ../ --> move back
nano .gitignore --> create a .gitignore
    mp3/
    head/mp3
    head/[0-9]*
    node_modules/
    package-lock.json
    package.json
